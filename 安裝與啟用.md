# SVN AI Helper — 安裝與啟用說明

本說明寫給不熟悉網頁環境的工程師，照著步驟做即可在本機安裝並啟動此應用。

---

## 零、最簡單：一鍵啟動（建議先試）

此壓縮包已內含 **Windows 嵌入式 Python 3.13（python-3.13）**，下載解壓後即可使用，**不需要另外安裝 Python**。

1. 取得專案壓縮包，解壓到任意資料夾（例如 `D:\Tools\svn-ai-helper`）。
2. 複製 **`.env.example`** 為 **`.env`**，編輯 `.env` 至少填入 **`OPENAI_API_KEY`**。
3. **雙擊專案目錄內的 `start.bat`**。

第一次執行時會：

- 自動使用內建的 `python-3.13\python.exe`
- 自動安裝 pip（若尚未安裝且包內有 `get-pip.py`）
- 自動安裝 `requirements.txt` 中的套件
- 啟動服務在 **http://127.0.0.1:8001**

之後每次只要再雙擊 `start.bat` 即可啟動（若 `.env` 不變，通常不用再做任何設定）。

若你希望改用自己安裝的 Python 或進階手動安裝方式，請從下方「一、事前準備」開始閱讀。

---

## 一、事前準備

### 1.1 安裝 Python

本程式需要 **Python 3.10 以上**（建議 3.11 或 3.12）。

- **若尚未安裝 Python：**
  1. 開啟 [https://www.python.org/downloads/](https://www.python.org/downloads/)
  2. 下載 Windows 安裝檔並執行
  3. **重要：** 安裝時勾選 **「Add Python to PATH」**
  4. 完成後關閉並重新開啟「命令提示字元」或 PowerShell

- **檢查是否安裝成功：** 開啟命令提示字元，輸入：
  ```bash
  python --version
  ```
  應顯示類似 `Python 3.11.x` 或 `Python 3.12.x`。

### 1.2 本機已有 SVN（選用）

若你要讓 AI 幫你執行 `svn checkout`、`svn merge` 等指令，本機需已安裝 **TortoiseSVN** 或 Subversion 指令列工具，並在稍後設定檔中填寫正確的 `SVN_EXE_PATH`。若尚未安裝，可先略過，仍可先跑起網頁與對話。

---

## 一.1、嵌入式（免安裝）Python 佈署

若你使用的是 **Windows 嵌入式（embeddable）Python**（免安裝、解壓即用），請依下列步驟佈署；**不需要**把 Python 加入系統 PATH。

1. **取得嵌入式 Python**
   - 至 [python.org/downloads](https://www.python.org/downloads/) 下載 **Windows embeddable package (64-bit)**（例如 `python-3.11.x-embed-amd64.zip`）。
   - 解壓到一個目錄，例如 `D:\Tools\python-3.11-embed`。以下以 `D:\Tools\python-3.11-embed` 為例，請依你的路徑替換。

2. **啟用 pip（嵌入式版預設無 pip）**
   - 用記事本開啟解壓目錄內的 **`python311._pth`**（檔名依版本為 `python3xx._pth`）。
   - 找到 `#import site` 這一行，**刪掉開頭的 `#`**，存檔（讓 Python 會載入 site-packages）。
   - 下載 [get-pip.py](https://bootstrap.pypa.io/get-pip.py) 到該目錄，在命令提示字元執行：
     ```cmd
     D:\Tools\python-3.11-embed\python.exe get-pip.py
     ```
   - 若執行時出現權限或路徑錯誤，可改為安裝到使用者目錄：
     ```cmd
     D:\Tools\python-3.11-embed\python.exe get-pip.py --user
     ```

3. **取得專案並安裝依賴**
   - 將 **svn-ai-helper** 專案複製或 clone 到目錄，例如 `D:\Tools\svn-ai-helper`。
   - 在該目錄下用**嵌入式 Python 的完整路徑**安裝套件（不需建立 venv）：
     ```cmd
     cd /d D:\Tools\svn-ai-helper
     D:\Tools\python-3.11-embed\python.exe -m pip install -r requirements.txt
     ```

4. **設定 .env**
   - 同「四、設定環境變數」：複製 `.env.example` 為 `.env`，至少設定 `OPENAI_API_KEY`。

5. **啟動服務**
   - 一律用**同一支 python.exe** 執行 uvicorn，例如：
     ```cmd
     D:\Tools\python-3.11-embed\python.exe -m uvicorn app:app --host 127.0.0.1 --port 8000
     ```
   - 瀏覽器開啟 **http://127.0.0.1:8000** 即可使用。

6. **（選用）寫成批次檔方便日後啟動**  
   本專案已附 **`start.bat`**（會自動偵測 Python 並建 venv）。若你使用嵌入式 Python，可自行編輯 `start.bat`，將其中的 `python` 改為嵌入式 Python 的完整路徑；或直接在命令列用該路徑執行：  
   `D:\Tools\python-3.11-embed\python.exe run.py`

若你是一般安裝版 Python（已加入 PATH），請略過本節，直接從下方「二、取得專案程式」開始。

---

## 二、取得專案程式

將 **svn-ai-helper** 整個資料夾複製到你要放置的目錄，例如：

- `D:\Tools\svn-ai-helper`

或由版控 clone 到該目錄。記住這個路徑，以下步驟都在此資料夾內進行。

---

## 三、建立 Python 虛擬環境並安裝套件

1. 開啟 **命令提示字元** 或 **PowerShell**。

2. 切換到專案目錄（請依你的實際路徑修改）：
   ```bash
   cd D:\Tools\svn-ai-helper
   ```

3. 建立虛擬環境（只需做一次）：
   ```bash
   python -m venv venv
   ```

4. 啟動虛擬環境：
   - **命令提示字元：**
     ```bash
     venv\Scripts\activate.bat
     ```
   - **PowerShell：**
     ```bash
     .\venv\Scripts\Activate.ps1
     ```
   成功時，命令列前面會出現 `(venv)`。

5. 安裝所需套件：
   ```bash
   pip install -r requirements.txt
   ```
   等待安裝完成，沒有錯誤即可。

---

## 四、設定環境變數（.env）

1. 在專案目錄下找到 **`.env.example`** 檔案（若沒看到，可能是被隱藏，需在檔案總管開啟「顯示隱藏的檔案」）。

2. **複製** `.env.example`，並將複本**重新命名**為 **`.env`**（副檔名就是 `.env`，沒有 .txt）。

3. 用記事本或任何文字編輯器**開啟 `.env`**，至少修改下面這一項：
   - **`OPENAI_API_KEY`**：改成你的 OpenAI API 金鑰（向公司或 OpenAI 申請）。  
     範例：`OPENAI_API_KEY=sk-proj-xxxxxxxxxxxx`

4. 其餘變數可先不改，程式會用內建預設值。若你部門的 SVN 網址、工作目錄或 branch 命名不同，再依 `.env.example` 裡的註解修改，例如：
   - `SVN_BASE_URL`：你們的 SVN 伺服器根網址
   - `WORKSPACE_ROOT`：希望 checkout 與檔案操作限制在哪個本機目錄（例如 `D:\Projects\AutoMerge`）
   - `SVN_EXE_PATH`：本機 `svn.exe` 的完整路徑（例如 TortoiseSVN 的 `C:\Program Files\TortoiseSVN\bin\svn.exe`）
   - `SVN_BRANCH_DIR_NAMES`：若 branch 目錄不叫 `branches`，可改為 `branch` 或 `branch,br` 等

5. **存檔後關閉** `.env`。

---

## 五、啟動應用程式

1. 確認虛擬環境已啟動（命令列前有 `(venv)`）。若沒有，請先執行：
   ```bash
   cd D:\Tools\svn-ai-helper
   .\venv\Scripts\Activate.ps1
   ```
   （命令提示字元請用 `venv\Scripts\activate.bat`）

2. 輸入以下指令啟動服務：
   ```bash
   uvicorn app:app --host 127.0.0.1 --port 8000
   ```

3. 若無錯誤，會看到類似：
   ```
   INFO:     Uvicorn running on http://127.0.0.1:8000
   ```
   表示服務已啟動。

4. 開啟瀏覽器，網址列輸入：
   ```
   http://127.0.0.1:8000
   ```
   或
   ```
   http://127.0.0.1:8000/
   ```

5. 應會看到 **SVN AI Helper** 的首頁，左側可輸入自然語言指令、右側會顯示 AI 執行的 svn 指令紀錄。

---

## 六、日常使用：如何「啟用」應用

每次要使用時：

1. 開啟命令提示字元或 PowerShell。
2. 切換到專案目錄並啟動虛擬環境：
   ```bash
   cd D:\Tools\svn-ai-helper
   .\venv\Scripts\Activate.ps1
   ```
3. 執行：
   ```bash
   uvicorn app:app --host 127.0.0.1 --port 8000
   ```
4. 用瀏覽器開啟 **http://127.0.0.1:8000** 即可使用。
5. 用畢後在執行 uvicorn 的那個視窗按 **Ctrl+C** 即可關閉服務。

---

## 七、常見問題

- **Q： port 8000 被佔用怎麼辦？**  
  可改用其他 port，例如：
  ```bash
  uvicorn app:app --host 127.0.0.1 --port 8080
  ```
  瀏覽器改開 **http://127.0.0.1:8080**。

- **Q： 想讓同網段其他電腦也能連？**  
  可改成：
  ```bash
  uvicorn app:app --host 0.0.0.0 --port 8000
  ```
  其他電腦用 `http://你這台電腦的IP:8000` 連線（需注意防火牆與資安）。

- **Q： 沒有 OpenAI API key？**  
  必須先取得 key 並寫入 `.env` 的 `OPENAI_API_KEY`，否則對話會無法使用。

- **Q： 執行 `pip install -r requirements.txt` 失敗？**  
  請確認已安裝 Python 且已勾選「Add Python to PATH」，並在專案目錄下先執行 `python -m venv venv` 再 `venv\Scripts\activate` 後重試。

---

## 八、其他打包方式（讓下載者更好安裝）

除了手動安裝，還可選以下方式，讓取得專案的人更容易執行：

| 方式 | 適用對象 | 說明 |
|------|----------|------|
| **一鍵啟動 `start.bat`** | 已安裝 Python 的使用者 | 專案已內建。下載者只要設好 `.env`，雙擊 `start.bat`，首次會自動建 venv 與安裝套件。 |
| **單檔執行檔（.exe）** | 不想裝 Python 的使用者 | 維護者用 PyInstaller 打包後釋出；下載者解壓、設 `.env`、執行 `.exe` 即可。見下方「單檔執行檔」。 |
| **Docker** | 已安裝 Docker 的使用者 | 專案內含 `Dockerfile`。下載者執行 `docker compose up` 或 `docker build` + `docker run` 即可。見下方「Docker」。 |
| **便攜壓縮包** | 需帶著走的環境 | 將嵌入式 Python + 已安裝套件 + 專案程式一起打成 zip；下載者解壓、設 `.env`、執行批次檔。需自行建包或依「一之一」手動製作。 |

### 單檔執行檔（PyInstaller）

**維護者建置：** 在專案目錄執行（需先 `pip install pyinstaller`）：

```cmd
pyinstaller --onefile --name SVN_AI_Helper run.py
```

會產出 `dist\SVN_AI_Helper.exe`。釋出時請一併提供 **`.env.example`**，並說明下載者須複製為 `.env`、填入 `OPENAI_API_KEY`，且將 `.env` 與 `.exe` 放在同一目錄後執行。

**下載者使用：** 解壓取得 `SVN_AI_Helper.exe` 與 `.env.example` → 複製 `.env.example` 為 `.env` 並填寫 API key → 雙擊 `SVN_AI_Helper.exe` → 瀏覽器開啟 http://127.0.0.1:8000。

### Docker

專案根目錄有 **`Dockerfile`**。使用方式範例：

```bash
# 建置映像
docker build -t svn-ai-helper .

# 執行（需掛載 .env 或傳入環境變數）
docker run --rm -p 8000:8000 --env-file .env svn-ai-helper
```

若有 `docker-compose.yml`，可改為 `docker compose up`。注意：容器內無法直接使用本機的 TortoiseSVN，若需執行 svn 指令，需額外掛載路徑或改在 host 跑應用。

---

## 九、目錄結構參考

```
svn-ai-helper/
├── app.py              # 主程式
├── run.py               # 單一啟動入口（python run.py / 打包用）
├── start.bat            # 一鍵啟動（自動 venv + pip）
├── requirements.txt    # Python 套件清單
├── .env                 # 你的設定（勿上傳版控）
├── .env.example         # 設定範例
├── venv/                # 虛擬環境（pip install 或 start.bat 產生）
├── Dockerfile           # Docker 建置用（選用）
└── 安裝與啟用.md        # 本說明
```

完成以上步驟後，即可在本機安裝並啟用此應用。
